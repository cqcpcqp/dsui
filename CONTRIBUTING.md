# Developer Contribution Guide

## Start

First start:

```bash
yarn run init
```

After that, just run the following command:

```bash
yarn run dev
```

example vue3 + vite
packages rollup
publish lerna
