# Developer Contribution Guide

## Start

First start:

```bash
yarn init
```

After that, just run the following command:

```bash
yarn dev
```

Generate color:

```bash
cd packages/ds-ui
yarn color-generate
```
