{
  "name": "dsui",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "init": "yarn && cd example && yarn",
    "dev": "cd example && yarn run dev",
    "build": "cd packages/ds-ui && yarn run dev",
    "build:prod": "cd packages/ds-ui && yarn build-prod && cd ../.. && cd example && yarn build",
    "start": "concurrently \"yarn:build\" \"yarn:dev\"",
    "test:core": "cd packages/ds-core && vitest"
  },
  "author": "cqcpcqp",
  "license": "ISC",
  "type": "module",
  "workspaces": [
    "packages/*"
  ]
}
